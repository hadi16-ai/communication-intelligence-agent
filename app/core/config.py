"""Application settings loaded from environment variables (implemented in M1).

Will define DEFAULT_GEMINI_MODEL = "gemini-3-flash-preview" and a Settings
class (pydantic-settings) exposing it as an overridable `gemini_model` field.
"""
