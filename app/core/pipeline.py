"""Orchestrates READ -> CLASSIFY -> DECIDE -> STORE (implemented in M7).

READ source is swappable: synthetic fixtures first (M7), live Gmail plugged
in later (M10) without changes to CLASSIFY/DECIDE/STORE.
"""
