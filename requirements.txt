# Dashboard
# Pinned to the exact minor version this app is developed/tested against —
# streamlit's AppTest harness has changed relative-path resolution behavior
# across versions before, which silently broke this project's test suite.
streamlit~=1.63.0

# Gemini (current recommended SDK, not the deprecated google-generativeai)
google-genai~=2.23.0

# Gmail API + OAuth (read-only in Version 1)
google-api-python-client~=2.200.0
google-auth~=2.58.0
google-auth-oauthlib~=1.4.1
google-auth-httplib2~=0.4.2
# google-api-python-client pulls in protobuf>=7 by default, which conflicts
# with Streamlit's protobuf<7 requirement — pin explicitly so a fresh
# `pip install -r requirements.txt` doesn't silently break the dashboard.
protobuf<7

# Schemas / config
pydantic~=2.13.0
pydantic-settings~=2.15.0
python-dotenv~=1.2.0

# Reliability: controlled retries around transient API failures
# (rate limits, temporary network errors) for Gmail/Gemini calls
tenacity~=9.1.0

# Testing
pytest~=9.1.0
pytest-mock~=3.15.0
